function addMenu(){
	var myMenuBar 	= QApplication.activeWindow().menuBar()
	var myMenu 		= myMenuBar.addMenu("my shiny new menu")
	MessageLog.trace("we should now have a new menu")
}

how to do update shortcuts using similar process

where is the harmony on startup location? ( to build menus this way )



packages location C:\Program Files (x86)\Toon Boom Animation\Toon Boom Harmony 20 Premium\Plugins\ScriptingInterfaces\resources\packages


